import { useState } from 'react'
import { motion } from 'framer-motion'

export default function ClinicalNote({ note }) {
  const [copied, setCopied] = useState(false)

  const handleCopy = () => {
    navigator.clipboard.writeText(note)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000)
  }

  if (!note) return null

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.7, duration: 0.5 }}
      className="bg-white rounded-xl shadow-lg p-8"
    >
      <h3 className="text-2xl font-bold text-slate-800 mb-4">
        ✍️ Generated Clinical Note
      </h3>

      <p className="text-sm text-slate-600 mb-4">
        Ready-to-copy documentation for your patient's medical record:
      </p>

      <motion.div
        initial={{ opacity: 0, y: 10 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.8 }}
        className="bg-slate-50 border-2 border-slate-200 rounded-lg p-6 mb-6"
      >
        <p className="text-slate-800 leading-relaxed whitespace-pre-wrap font-mono text-sm">
          {note}
        </p>
      </motion.div>

      <motion.button
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.85 }}
        onClick={handleCopy}
        className={`w-full py-3 font-semibold rounded-lg transition ${
          copied
            ? 'bg-success text-white'
            : 'bg-primary text-white hover:shadow-lg transform hover:-translate-y-1'
        }`}
      >
        {copied ? '✅ Copied to Clipboard!' : '📋 Copy to Clipboard'}
      </motion.button>

      <p className="text-xs text-slate-500 mt-4 text-center">
        Note generated by Claude AI • For documentation purposes • Clinical judgment required
      </p>
    </motion.div>
  )
}
